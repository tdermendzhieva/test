package com.allie.data.repository;

/**
 * Created by andrew.larsen on 10/17/2016.
 */
//@RunWith(SpringRunner.class)
//@DataJpaTest
public class LocationTelemetryRepositoryTest {
/*
    @Autowired
    private TestEntityManager testEntityManager;
    @Autowired
    private LocationTelemetryRepository repository;

    List<LocationTelemetry> locationTelemetries;
    LocationTelemetry locationTelemetry;
    private String allieId;
    private double[] location = {10, 12};;
    private BSONTimestamp bsonTimestamp;
    @Before
    public void setup(){
        locationTelemetries = new ArrayList<>();
        locationTelemetry = new LocationTelemetry();
        allieId = "testid";
        bsonTimestamp  = new BSONTimestamp();
    }
    @Test
    public void testFindByAllieId() throws Exception {
        locationTelemetry.setAllieId(allieId);
        locationTelemetry.setLocation(location);
        locationTelemetry.setTimestamp(bsonTimestamp);
        locationTelemetries.add(locationTelemetry);
        this.repository.insert(locationTelemetries);
        this.testEntityManager.find(LocationTelemetry.class, locationTelemetry.getAllieId());
    }
*/
}